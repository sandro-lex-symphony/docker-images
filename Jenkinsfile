node {
    stage('Preparation') {
        echo '### Stage preparation'
    }

    stage('Docker build') {
        echo '### Going to docker build'
    }

    stage('Vuln Scan') {
        echo '### Going to scan it'
    }
}
